{
  "name": "example-moonshine-tachyons",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "npx nodemon -e yml --exec distill --watch",
    "build": "distill"
  },
  "dependencies": {
    "@econify/moonshine-css": "file:../.."
  }
}
